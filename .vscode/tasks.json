{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Mnemosyne: Startup",
      "type": "shell",
      "command": "${command:python.interpreterPath}",
      "args": [
        "${workspaceFolder}/.mnemosyne/startup.py"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "runOn": "folderOpen",
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}
